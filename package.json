{
  "name": "qrty",
  "version": "2.0.0",
  "description": "QR code decoder using screen capture or file upload",
  "scripts": {
    "start": "node scripts/dev-server.js",
    "test": "jest",
    "lint": "eslint src/**/*.js",
    "format": "prettier --write \"src/**/*.{js,html,css}\" \"scripts/**/*.js\"",
    "format:check": "prettier --check \"src/**/*.{js,html,css}\" \"scripts/**/*.js\"",
    "typecheck": "tsc --noEmit",
    "build:renderer": "tsc -p tsconfig.renderer.json && cp build/renderer/renderer.js src/renderer.js && rm -rf build/renderer",
    "generate:icons": "node scripts/generate-icons.js",
    "generate:og-image": "node scripts/generate-og-image.js",
    "generate:qr-code": "node scripts/generate-qr-code.js",
    "generate:all": "pnpm run generate:icons && pnpm run generate:og-image && pnpm run generate:qr-code",
    "build": "pnpm run generate:all"
  },
  "keywords": [
    "qr",
    "qr-code",
    "decoder",
    "scanner"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@babel/preset-env": "^7.28.5",
    "@babel/preset-typescript": "^7.28.5",
    "@napi-rs/canvas": "^0.1.81",
    "@types/jest": "^30.0.0",
    "@types/jsdom": "^27.0.0",
    "@types/node": "^24.10.0",
    "@zxing/library": "^0.21.3",
    "eslint": "^8.57.1",
    "http-server": "^14.1.1",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "jsbarcode": "^3.12.1",
    "playwright": "^1.56.1",
    "prettier": "^3.6.2",
    "qrcode": "^1.5.4",
    "ts-jest": "^29.4.5",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "jsqr": "^1.4.0"
  }
}
